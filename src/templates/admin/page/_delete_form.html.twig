<form method="POST" action="{{ path('simsy_cms_admin_page_delete', { id: page.id }) }}">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete_page_' ~ page.id) }}">
    <button type="submit" class="btn btn-sm btn-danger">{{ 'simsy_cms.delete'|trans }}</button>
</form>